# CKQA Codex Project Config

本仓库当前有五块主要目录。知识生产与检索主线仍然是两个 Python 子项目，Java 后端负责一期业务编排，前端仍以原型为主：

- `pdf_ingest/`：课程 PDF 解析、清洗、标准化导出。
- `graphrag_pipeline/`：消费导出结果，建索引并提供问答 API。
- `frontend/apps/student-app/`：学员端 Vue 3 + Vite 原型，已有最小 Axios 请求层，但尚未接入稳定业务契约。
- `frontend/apps/admin-app/`：管理员端/教师端共用控制台前端，已完成 Element Plus + Pinia + Sass 样式基座、主题系统、工作台、系统健康页和核心运维页真实接入，但仍不是知识生产主链路入口。
- `backend/ckqa-back/`：Java 21 + Spring Boot 4 一期编排后端，提供 `/api/v1` 课程、PDF、索引、异步问答与系统健康检查接口，仍依赖两个 Python 模块完成真实处理。

## 必读顺序

开始改动前，优先阅读：

1. `AGENTS.md`
2. `pdf_ingest/CLAUDE.md`
3. `graphrag_pipeline/CLAUDE.md`

按需补充阅读：

- `docs/标准化导出验证说明.md`
- `pdf_ingest/docs/MinerU PDF Parser.md`
- `graphrag_pipeline/README.md`
- `frontend/apps/student-app/README.md`
- `frontend/apps/admin-app/README.md`
- `docs/admin-teacher-frontend-structure.md`（涉及 admin-app 页面结构、路由或 RBAC 时）
- `backend/ckqa-back/README.md`
- `docs/student-backend-graphrag-api-contract.md`（涉及 student-app、Java 后端或 GraphRAG API 联调时）

如果文档和代码不一致，以代码与当前依赖配置为准，并在结果中指出分叉点。

## 先记住这几个现实情况

- `graphrag_pipeline/pyproject.toml` 当前固定的是 `graphrag==3.0.9`，仓库内涉及版本判断时统一以它为准。
- `graphrag_pipeline/utils/main.py` 当前已经收口为纯 GraphRAG CLI 查询包装层，不再尝试 2.x 风格的内部 API。
- `pdf_ingest` 导出时会同时生成标准化中间结果 `normalized_docs.json`，以及面向 GraphRAG 的 `section_docs.json` / `page_docs.json`。
- `frontend/apps/admin-app/` 仍不是主业务链路；`backend/ckqa-back/` 已经承担 Java 编排，但真实解析、索引和问答能力仍由两个 Python 模块提供。

## 仓库级工作规则

- 默认使用中文沟通、注释、提交说明与文档更新。
- 优先做最小范围修改，不顺手重构无关模块。
- 修改前先确认自己处在哪个子项目目录，避免混淆两套 Python 环境、一套 Java 编排后端和两套 Node 前端原型。
- 不要随意编辑 `.env`、`output/`、`reports/`、`cache/`、`node_modules/`、`*.egg-info/`、IDE 元数据和其它生成物。
- 不要传播或复用仓库中的真实密钥、Token、账号密码；如果发现硬编码敏感值，应视为风险点而不是示例配置。

## 模块速览

### 1. `pdf_ingest/`

核心入口：

- `pdf_ingest/scripts/pdf_processor/mineru_parser.py`

关键模块：

- `block_model.py`：统一 MinerU 解析块结构
- `block_renderer.py`：按块类型渲染文本
- `text_cleaner.py`：清洗页眉页脚与噪声文本
- `graphrag_exporter.py`：输出 `normalized_docs.json`、`section_docs.json`、`page_docs.json`
- `db_service.py`：MySQL 服务层
- `storage_service.py`：MinIO 服务层

常用命令：

- `conda activate courseKg`
- `pip install -e .`
- `pip install -e ".[dev]"`
- `python scripts/pdf_processor/mineru_parser.py upload <course_id> -f <pdf> --parse`
- `python scripts/pdf_processor/mineru_parser.py parse <course_id> --material-id <id>`
- `python scripts/pdf_processor/mineru_parser.py export-graphrag <course_id> --material-id <id> --mode section`
- `python -m pytest tests/`

实现注意事项：

- 运行配置主要来自 `pdf_ingest/.env`，由 `Config.from_env()` 自动查找加载。
- 当前共享开发环境里的 `courseKg` 已安装 `pytest`，因此仓库内日常验证可直接运行测试；如果重建环境，仍建议使用 `pip install -e ".[dev]"` 保持可复现。
- 解析状态依赖 MySQL：`pending -> processing -> done/failed`。
- 同一课程可绑定多份 PDF；多文件课程下优先传 `--material-id`，`--file-id` / `--file-name` 仅保留兼容入口。
- `export-graphrag` 是对下游最重要的接口，改字段、命名或 MinIO 路径时必须检查 `graphrag_pipeline` 兼容性。

### 2. `graphrag_pipeline/`

核心入口：

- `graphrag_pipeline/utils/main.py`

关键文件：

- `pyproject.toml`：依赖与版本真相，当前固定 `graphrag==3.0.9`
- `scripts/build_prompt_tuning_samples.py`：构建 prompt tuning 样本
- `scripts/build_audit_extraction_set.py`：构建 audit 校准集
- `scripts/generate_candidate_prompts.py`：生成候选 Prompt 与 manifest
- `scripts/run_graphrag_prompt_tune.py`：封装 GraphRAG 官方 prompt-tune
- `settings.yaml`：GraphRAG CLI 的索引配置
- `.env`：GraphRAG CLI 使用的环境变量
- `utils/fetch_from_minio.py`：从 MinIO 拉取 `section_docs.json` / `page_docs.json` / `normalized_docs.json`
- `utils/neo4jTest.py`：导入 Neo4j
- `utils/graphrag3dknowledge.py`：3D 可视化

常用命令：

- `conda activate graphrag-oneapi`
- `pip install -e ".[all]"`
- `python -m pytest tests/`
- `python utils/fetch_from_minio.py <course_id> --clean`
- `python utils/fetch_from_minio.py <course_id> --pdf-file-id <id> --clean`
- `python utils/fetch_from_minio.py <course_id> --pdf-file-id <id> --json-file normalized_docs.json --clean`
- `graphrag index --root .`
- `graphrag query --root . --method local "问题"`
- `graphrag query --root . --method global "问题"`
- `graphrag query --root . --method drift "问题"`
- `graphrag query --root . --method basic "问题"`
- `python utils/main.py`

实现注意事项：

- GraphRAG 输入主流程已经是 `json`，不再是旧版 `txt` 主流程。
- 仓库根目录 `scripts/` 只保留仓库级工具；GraphRAG 模块专属脚本统一放在 `graphrag_pipeline/scripts/`。
- `fetch_from_minio.py` 会尽量把标准化字段提升到顶层，便于 GraphRAG metadata 收集。
- 当前共享开发环境里的 `graphrag-oneapi` 已安装 `pytest`，因此仓库内可直接跑测试；如果重建环境，请在安装项目依赖后额外补装 `pytest`。
- `utils/main.py` 会优先读取仓库内 `.env` / 当前环境变量，默认读取仓库内 `output/`，并统一委托 `graphrag query` 执行查询。
- `output/` 下的 parquet 与 `output/lancedb/` 都是 API 服务必需品。
- 当前公开 API 模型名是 `graphrag-local-search:latest`、`graphrag-global-search:latest`、`graphrag-drift-search:latest`、`graphrag-basic-search:latest`；`full-model:latest` 已归档为后续扩展模式，不再对外宣称可用。
- 修改入口文档、关键脚本或版本基线后，可运行 `python scripts/audit_repo_drift.py --strict` 做仓库级审计。

### 3. `frontend/apps/student-app/`

- 当前是学员端 Vue 3 + Vite 原型，技术栈包含 Element Plus、Pinia、Vue Router、Sass。
- 已有 `src/axios/index.js` 最小请求层，环境变量见 `.env.example`。
- 常用命令：
  - `cd frontend/apps/student-app`
  - `pnpm install`
  - `pnpm dev`
  - `pnpm build`
  - `node --test tests/*.test.js`
- 当前页面仍以本地 store、静态内容和显式“未开放”路由为主，不要假设已经接入稳定后端契约。

### 4. `frontend/apps/admin-app/`

- 当前是独立的 Vue 3 + Vite 管理员/教师控制台前端，技术栈已包含 Element Plus、Pinia 与 Sass。
- 常用命令：
  - `cd frontend/apps/admin-app`
  - `pnpm install`
  - `pnpm test`
  - `pnpm build`
  - `pnpm dev`
- 目前已完成控制台壳层、Element Plus + Pinia + Sass 样式基座、主题系统、工作台、健康页、登录页与状态页，以及课程/资料/知识库/构建向导/QA 冒烟验证等核心运维页的 Java `/api/v1` 真实接入；正式浏览器接口边界仍是 Java `/api/v1`。
- `node_modules/` 已出现在仓库中，但它不是源码，不要把依赖目录当成改动目标。

### 5. `backend/ckqa-back/`

- 当前是 Java 21 + Spring Boot 4.0.5 + MyBatis-Plus 3.5.16 一期编排后端。
- 统一响应格式为 `code / message / data / timestamp`，业务成功码为 `200`。
- 已落地 `/api/v1/system/health`、课程/PDF 查询、解析触发、GraphRAG 导出、索引任务、问答会话与异步问答任务接口。
- 异步问答通过 `graphrag_pipeline` 的 `/v1/query-tasks` 工作；跨服务任务时间对外统一按 `Asia/Shanghai` 的无偏移 `LocalDateTime` 字符串解释。
- 若用户请求未明确指向 Java 后端、`/api/v1`、编排或前端联调，优先排查两个 Python 模块。

## 跨模块主链路

1. `pdf_ingest` 解析课程 PDF。
2. `pdf_ingest` 导出 `normalized_docs.json` 与 GraphRAG 投影 JSON。
3. `graphrag_pipeline` 拉取 JSON 到 `input/`。
4. `graphrag_pipeline` 执行 `graphrag index --root .`。
5. `graphrag_pipeline/utils/main.py` 提供 OpenAI 兼容问答接口。

因此，凡是涉及导出字段、metadata 命名、对象存储路径或文件命名的改动，都要检查是否影响下游索引与查询。

## 修改优先级建议

- PDF 解析、上传、导出、标准化问题：先看 `pdf_ingest/`
- GraphRAG 索引、查询、API、Neo4j、可视化问题：先看 `graphrag_pipeline/`
- 学员端页面、路由、请求层：看 `frontend/apps/student-app/`
- 管理端页面或前端静态资源：看 `frontend/apps/admin-app/`
- Java API、Spring 配置、统一响应、异步问答编排：看 `backend/ckqa-back/`

## 交付时应说明

- 这次改动影响的是哪个模块
- 是否涉及跨模块接口兼容性
- 是否需要特定 conda 环境、Node、数据库、MinIO、Neo4j 或 OneAPI/OpenAI 兼容服务才能验证
- 如果没跑测试或没启动服务，要明确说明原因
