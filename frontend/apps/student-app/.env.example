# 学生端请求基础地址。
# 正式联调时应指向 Java 后端 /api/v1，而不是直接指向 GraphRAG Python 服务。
# 示例：VITE_API_BASE_URL=http://127.0.0.1:8080/api/v1
# 如果开发阶段使用 Vite 反向代理，建议代理前缀也保持为 /api/v1。
VITE_API_BASE_URL=/api/v1

# 请求超时时间，单位毫秒
VITE_API_TIMEOUT=10000
