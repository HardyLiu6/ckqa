from __future__ import annotations

from textwrap import dedent


SEMANTIC_PROMPT = dedent(
    """
    你是严格的课程问答评分员。请判断「学生答案」与「参考答案」在语义上是否一致。

    判分规则：
    - 1.0：核心结论与参考答案完全一致，关键实体、数值、关系都对得上。可以接受同义表达（例如「ε-邻域半径」与「eps」、「最小邻居数」与「MinPts」）。
    - 0.5：方向正确但缺少关键要点 / 含可纠正的小错误，或仅部分覆盖参考答案。
    - 0.0：与参考答案矛盾、答非所问、严重缺失要点。

    输出严格 JSON：
    {{
      "score": 1.0 | 0.5 | 0.0,
      "rationale": "<= 60 字"
    }}

    问题：{question}
    参考答案：{gold_answer_summary}
    学生答案：
    \"\"\"
    {answer}
    \"\"\"
    """
).strip()


FAITHFULNESS_PROMPT = dedent(
    """
    你是严格的课程问答忠实性评分员。请判断「学生答案」中的每一条事实陈述是否能在「证据原文」里找到支撑。

    判分规则：
    - 1.0：核心陈述都能在证据中找到支撑，且没有明显超出证据范围的扩写。
    - 0.5：主体结论能在证据中找到支撑，但包含少量超出证据范围的合理扩写，或部分细节缺乏直接依据。
    - 0.0：关键结论无法从证据支持，或存在明显与证据冲突的陈述。

    输出严格 JSON：
    {{
      "score": 1.0 | 0.5 | 0.0,
      "unsupported_claims": ["<最多 3 条不被支持的陈述>"]
    }}

    问题：{question}
    证据原文：
    {evidence}

    学生答案：
    \"\"\"
    {answer}
    \"\"\"
    """
).strip()


RETRIEVAL_PROMPT = dedent(
    """
    你是严格的课程问答证据利用评分员。请根据「证据原文」判断「学生答案」是否有效利用了与问题最相关的 gold 证据。

    判分规则：
    - 1.0：答案抓住了 gold 证据中的核心信息，主要结论明显建立在这些证据之上。
    - 0.5：答案只利用了部分 gold 证据，或结论大体相关但证据利用不充分。
    - 0.0：答案几乎没有体现 gold 证据中的关键内容，或主要依赖无关扩写。

    输出严格 JSON：
    {{
      "score": 1.0 | 0.5 | 0.0,
      "rationale": "<= 60 字"
    }}

    问题：{question}
    证据原文：
    {evidence}

    学生答案：
    \"\"\"
    {answer}
    \"\"\"
    """
).strip()
